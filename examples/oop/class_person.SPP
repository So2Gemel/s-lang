class Person {
    let name: string;

    func init(n: string) {
        self.name = n;
    }

    func hello() {
        print("مرحبًا، أنا " + self.name);
    }
}

func main() {
    let p: Person = Person("سارة");
    p.hello();
}
